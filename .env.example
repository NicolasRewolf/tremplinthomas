# ─── Frontend (Vite — exposé au navigateur) ─────────────────────────────────
# Crée .env.local (gitignoré) et renseigne ces variables.
VITE_SUPABASE_URL=https://uxmblaaadtwoggbknpze.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_xxx   # publishable key, lecture seule via RLS

# ─── Crawler Python (crawler/.env — NE PAS exposer au navigateur) ───────────
# Voir crawler/.env.example pour les variables côté backend.
